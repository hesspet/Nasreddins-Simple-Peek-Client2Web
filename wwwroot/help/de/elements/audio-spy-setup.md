# Audio-Spy-Setup

Hier pflegst du bis zu 100 Mappings. Schlüsselwörter und Befehle werden kommasepariert eingegeben. Schlüsselwörter unterstützen exakte Wörter, Wildcards mit `*` und `?` sowie Regex-Ausdrücke im Format `/.../`.

Das Beispiel `auto,car,ma*ine,/aut./` sendet die Befehle `CL,EQ`.
